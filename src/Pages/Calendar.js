import React, { Component } from "react";
import { withRouter } from "react-router-dom";

class Calendar extends Component {
  render() {
    return (
      <div>
        <h1>Put your calendar logic here</h1>
      </div>
    );
  }
}

export default withRouter(Calendar);
